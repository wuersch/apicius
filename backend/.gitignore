# Backend (Quarkus / Maven) build outputs only.
# OS, editor, log, and env ignores live in the repo-root .gitignore.

# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
release.properties
.flattened-pom.xml

# Quarkus CLI plugins
/.quarkus/cli/plugins/

# Dev TLS certificates
.certs/
